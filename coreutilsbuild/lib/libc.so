/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-littlearm)
GROUP ( /mnt/SDCARD/App/coreutilsbuild/lib/libc.so.6 /mnt/SDCARD/App/coreutilsbuild/lib/libc_nonshared.a  AS_NEEDED ( /mnt/SDCARD/App/coreutilsbuild/lib/ld-linux-armhf.so.3 ) )
